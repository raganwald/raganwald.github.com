// Generated by CoffeeScript 1.6.1
(function() {

  describe("idiots", function() {
    var I;
    I = function(x) {
      return x;
    };
    return it("should respond to any bird with the same bird", function() {
      expect(I('polly')).toEqual('polly');
      return expect(I(I)).toEqual(I);
    });
  });

  describe("Kestrels", function() {
    var K;
    K = function(x) {
      return function(y) {
        return x;
      };
    };
    return it("responds to the Cardinal with a bird fixated on the Cardinal", function() {
      var B, C, FC, R;
      C = 'Cardinal';
      R = 'Robin';
      B = 'Bluebird';
      FC = K(C);
      expect(FC(C)).toEqual(C);
      expect(FC(R)).toEqual(C);
      expect(FC(B)).toEqual(C);
      return expect(FC(FC)).toEqual(C);
    });
  });

}).call(this);
